{
  "name": "rts-agents",
  "version": "1.0.0",
  "description": "RTS Agents Dashboard - Monitor Antigravity CLI, Jules, and Cursor Cloud Agents",
  "main": "main.js",
  "scripts": {
    "start": "npm run build && cross-env ELECTRON_DISABLE_SANDBOX=1 electron .",
    "dev": "concurrently -k \"vite\" \"wait-on http://localhost:5173 && cross-env VITE_DEV_SERVER_URL=http://localhost:5173 ELECTRON_DISABLE_SANDBOX=1 electron .\"",
    "build": "vite build",
    "build:web": "vite build",
    "deploy:web": "npm run build && wrangler deploy",
    "check:web": "npm run build && wrangler deploy --dry-run",
    "dev:headless": "node headless.js",
    "test": "jest",
    "test:ci": "node tests/unit/web-platform.verify.mjs && jest --testPathIgnorePatterns=integration",
    "test:e2e": "npm run build && cross-env E2E_NO_FOCUS=1 ELECTRON_DISABLE_SANDBOX=1 env -u DISPLAY xvfb-maybe playwright test",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "validate:docs": "node scripts/validate-docs.js",
    "validate:architecture": "node scripts/validate-architecture.js",
    "validate": "npm run validate:docs && npm run validate:architecture && npm run lint && npm run test:ci",
    "smoke:providers": "node scripts/provider-smoke.js",
    "docs:generate": "node -e \"console.log('No generators yet; see docs/generated/README.md')\""
  },
  "keywords": [
    "electron",
    "rts",
    "agents",
    "jules",
    "cursor",
    "dashboard"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "dompurify": "^3.3.1",
    "electron-store": "^8.1.0",
    "sonner": "^2.0.8"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@tailwindcss/typography": "^0.5.19",
    "@types/jest": "^29.5.14",
    "@vitejs/plugin-react": "^4.2.1",
    "autoprefixer": "^10.4.17",
    "concurrently": "^9.2.1",
    "cross-env": "^7.0.3",
    "electron": "^44.1.0",
    "eslint": "^9.33.0",
    "globals": "^16.3.0",
    "jest": "^29.7.0",
    "playwright": "^1.57.0",
    "postcss": "^8.4.35",
    "prettier": "^3.6.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "tailwindcss": "^3.4.0",
    "vite": "^5.1.0",
    "wait-on": "^7.2.0",
    "wrangler": "^4.127.1",
    "xvfb-maybe": "^0.2.1"
  }
}
